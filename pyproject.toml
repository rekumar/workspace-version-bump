[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "workspace-version-bump-hook-scripts"
version = "0.1.0"
description = "Scripts for a pre-commit hook for bumping versions in a workspace."